import "./fontRegistry.js";

const {
  DEFAULT_FONT_ID,
  FONT_REGISTRY,
  getFontOptions,
  normalizeFontId,
  normalizeTypography,
  resolveFontStack,
} = globalThis.AudioCaption;

export {
  DEFAULT_FONT_ID,
  FONT_REGISTRY,
  getFontOptions,
  normalizeFontId,
  normalizeTypography,
  resolveFontStack,
};
